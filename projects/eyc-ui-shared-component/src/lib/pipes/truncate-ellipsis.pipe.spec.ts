import { TruncateEllipsisPipe } from './truncate-ellipsis.pipe';

describe('TruncateEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
