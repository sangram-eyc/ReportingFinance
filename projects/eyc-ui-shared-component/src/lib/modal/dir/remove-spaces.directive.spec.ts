import { RemoveSpacesDirective } from './remove-spaces.directive';

describe('RemoveSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
