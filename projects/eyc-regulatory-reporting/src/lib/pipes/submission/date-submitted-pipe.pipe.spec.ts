import { DateSubmittedPipePipe } from './date-submitted-pipe.pipe';

describe('DateSubmittedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateSubmittedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
